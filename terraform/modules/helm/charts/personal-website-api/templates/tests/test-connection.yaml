apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "personal-website-api.fullname" . }}-test-connection"
  labels:
    {{- include "personal-website-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "personal-website-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
