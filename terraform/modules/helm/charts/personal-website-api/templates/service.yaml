apiVersion: v1
kind: Service
metadata:
  name: {{ include "personal-website-api.fullname" . }}
  labels:
    {{- include "personal-website-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort | default .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "personal-website-api.selectorLabels" . | nindent 4 }}
