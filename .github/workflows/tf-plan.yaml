name: Terraform Plan
on:
  workflow_call:
    secrets:
      kubeconfig:
        required: true
      aws_access_key_id:
        required: true
      aws_secret_access_key:
        required: true
      org_ssh_key:
        required: true
      pg_password:
        required: false
      pg_username:
        required: false
    inputs:
      environment:
        required: true
        type: string
      aws_region:
        required: false
        type: string
        default: us-east-1
      aws_assume_role_arn:
        required: true
        type: string
      aws_s3_backend_bucket:
        required: true
        type: string

jobs:
  tf-plan:
    runs-on: ubuntu-latest
    name: Terraform Plan
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.aws_access_key_id }}
          aws-secret-access-key: ${{ secrets.aws_secret_access_key }}
          aws-region: ${{ inputs.aws_region }}

      - name: Install dependencies
        run: |
          version=$(sudo curl -L -s https://dl.k8s.io/release/stable.txt)
          echo "Installing kubectl version $version"

          sudo curl -LO "https://dl.k8s.io/release/$version/bin/linux/amd64/kubectl"
          sudo install -o root -g root -m 0755 kubectl /usr/local/bin/kubectl

          mkdir -p $HOME/.kube

          echo "${KUBE_CONFIG}" | base64 -d > $HOME/.kube/config
        env:
          KUBE_CONFIG: ${{ secrets.kubeconfig }}

      - name: Setup SSH Agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.org_ssh_key }}

      - name: Run Terraform Plan
        working-directory: ./infrastructure/terraform/env/${{ inputs.environment }}
        run: |
          echo "Running Terraform plan..."
          terraform init -backend-config="bucket=${{ inputs.aws_s3_backend_bucket }}" \
            -backend-config="region=${{ inputs.aws_region }}" \
            -backend-config="assume_role={ role_arn = \"${{ inputs.aws_assume_role_arn }}\" }"

          terraform plan
        env:
          TF_VAR_postgres_password: ${{ secrets.pg_password }}
          TF_VAR_postgres_user: ${{ secrets.pg_username }}
          TF_VAR_aws_region: ${{ inputs.aws_region }}
          TF_VAR_aws_assume_role_arn: ${{ inputs.aws_assume_role_arn }}
