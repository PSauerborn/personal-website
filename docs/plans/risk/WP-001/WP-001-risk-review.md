# Risk Review: WP-001

Work Plan ID: WP-001
Risk Plan: `docs/plans/risk/WP-001/WP-001-risk-plan.md`
Review Date: 2026-08-13 (review loop iteration 1 — re-review after remediation)

Reviewed against the changeset defined by `docs/plans/manifests/WP-001-manifest.md`, including the
three remediations that landed after iteration 0: `TASK-CODE-REVIEW-REMEDIATION`,
`TASK-QC-REMEDIATION` and `TASK-SEC-REMEDIATION`. Accepted conditions carried into this review: no
container runtime exists in this environment (user-approved 2026-08-13), so neither image was
built and neither `make` target was executed; `base.admin_audit_log.api_key_id` was made NULLABLE
by user decision on 2026-08-13; the md5-derived `topic_article_link.id` was replaced with an
explicit fixture-supplied link id during execution.

**Iteration 0's single deviation (RISK-001) is closed.** It was resolved by
`TASK-CODE-REVIEW-REMEDIATION` rather than by a separate execution of
`docs/plans/tasks/WP-001/TASK-RISK-REMEDIATION.md`, which records the resolution. The same fix
also closed a latent RISK-002 failure mode that iteration 0 did not catch (see RISK-002 below).
All 22 risks are `mitigated` in this iteration; no remediation is required.

## Review Summary

One row per risk in the risk plan. Every `deviation-found` row cites evidence (file and line).

| Risk ID | Status | Evidence | Notes |
| --- | --- | --- | --- |
| RISK-001 | mitigated | `alembic/migrations/versions/0001_initial_base_schema.py:235-250,1175-1198`; `alembic/migrations/env.py:9-15,53-59,81-85`; `docs/db_schema.md:70-93`; `alembic/README.md:28-33` | **Deviation closed.** `drop_schema()` now emits a plain `execute_sql(f"DROP SCHEMA {SCHEMA} RESTRICT;")` (250) with no `DO $$` guard — the revision contains no conditional statement at all. `downgrade` (1175-1198) is the literal mirror: 20 `DROP TRIGGER` then 20 `DROP TABLE` over `reversed(TABLES)`, `drop_set_updated_at_function()`, `DOCUMENT_TYPE.drop`/`HTTP_METHOD.drop` with `checkfirst=False`, then `drop_schema()` last. `env.py` sets no `version_table_schema` in either `context.configure` call (53-59 offline, 81-85 online), so `alembic_version` lives outside `base` and the unconditional drop can succeed under `RESTRICT`. The residue claims were corrected: `docs/db_schema.md:75-84` now states the drop is unconditional and names `alembic_version`'s location outside `base`; `alembic/README.md:28-33` documents the new bookkeeping location. |
| RISK-002 | mitigated | `alembic/migrations/env.py:9-15,44-47,53-59,69-72,81-85`; `0001_initial_base_schema.py:41-65,1138-1172`; `docs/db_schema.md:78-84` | Previously-verified static properties still hold: `TABLES` (44-65) is an explicit FK-dependency order driving creation (1168-1169); schema, both enum types and `base.set_updated_at()` are created before any table (1151-1166); every raw statement is `base.`-qualified through the `SCHEMA` constant; longest identifiers are within 63 chars (`cv_stack_item_experience_link_set_updated_at` = 44) and no reserved word is used unquoted. **Additionally closed this iteration:** the `version_table_schema = "base"` pin was exactly the RISK-002 failure mode ("renders offline but fails at execution") — alembic creates its version table before the first revision body runs and creates no schema for it, so `alembic upgrade head` would have aborted against an empty database with `schema "base" does not exist`, never reaching `upgrade()`. `env.py` now configures no `version_table_schema` in either mode and records the reasoning in its module docstring (9-15) and in both function docstrings (44-47, 69-72); `docs/db_schema.md:78-84` states the same. The residual unverified runtime risk remains recorded in `docs/db_schema.md:849-877`. |
| RISK-003 | mitigated | `0001_initial_base_schema.py:41-65`; `docs/db_schema.md:95-99,103-421` | Unchanged by the remediations. All 20 named tables are present in `TABLES`, each with a dedicated builder driven by `TABLE_BUILDERS`, and each appears as its own Markdown table in `docs/db_schema.md` with columns, widths, nullability, defaults and constraints; the two artifacts agree table-for-table on the spot checks performed. |
| RISK-004 | mitigated | `scripts/seeding/README.md` §3, §8; `docs/plans/manifests/WP-001-manifest.md:43-93,105-110` | Unchanged: the 83-scenario census across all six feature files and all three tags stands, each catalogue row cites its motivating scenario, and the three unrepresentable expectations are surfaced as conflicts C-1/C-2/C-3 rather than worked around. No remediation touched `acceptance/**` or `docs/specs/**`; the manifest's changeset (including the remediation rows 38-41) contains no such path. |
| RISK-005 | mitigated | `scripts/seeding/README.md:317,472-502,611-633`; `scripts/seeding/fixtures/` | No fixture file was added, renamed or removed by any of the three remediations (manifest rows 38-40 touch only `src/`, `tests/`, Makefiles, READMEs, Dockerfiles, `.gitignore` and `.secrets.baseline`), so the published catalogue still matches the shipped tree exactly: 10 domain JSON files and 12 sidecars. Fixture IDs are unchanged and still declared a stable downstream-pinned contract. |
| RISK-006 | mitigated | `scripts/seeding/src/persistence.py:42,1032,1047`; `scripts/seeding/src/main.py:22-30,370-378,381-409,468-474` | Atomicity survived the security remediation. `execute_seed_plan` still takes an already-open connection, runs all truncations and inserts inside one `connection.transaction()` block (1032) and has the component's only `connection.commit()` (1047); a repository-wide search of `scripts/seeding/src/` finds no other `.commit()` and no `autocommit=True` (`open_connection` passes `autocommit=False`, 376). `seed()` still plans before connecting (401-403). The new `--yes` gate is evaluated *before* `seed()` is called (470-471) and its refusal path (`destructive_run_allowed`, 267-297) reads no fixture and opens no connection, so it strictly precedes — and cannot weaken — the ordering guarantee. |
| RISK-007 | mitigated | `scripts/seeding/README.md:611-633`; `scripts/seeding/src/persistence.py`; `scripts/seeding/tests/test_persistence.py` | Untouched by the remediations except for one docstring correction in `persistence.py` (manifest row 38), which changed no ordering constant. README §6 still records the FK-safe order independently with the per-constraint satisfaction table (`api_key` before `admin_audit_log` at 633, etc.), `test_persistence.py` still asserts `persistence.INSERT_ORDER == README_INSERT_ORDER` against the hand-transcribed README constant, and the zero-row truncate-set negative test remains. |
| RISK-008 | mitigated | `scripts/seeding/src/persistence.py`; `scripts/seeding/README.md` §5.2 | Unchanged: payload-comparing de-duplication raising `ConflictingFixtureRowError` on conflict, `validate_references` raising `UnresolvedReferenceError` during planning before any connection exists, ownership rules restated in README §5.2 and flagged as an orchestrator assumption. No remediation altered these functions' behaviour. |
| RISK-009 | mitigated | `scripts/seeding/src/models.py` (sidecar path handling and `size` derivation) | Unchanged by the remediations — `models.py` appears in no remediation row of the manifest. Absolute paths rejected, resolved path required to be within the resolved fixtures root, `Path.read_bytes()` binary read, `size` derived from byte length with empty files rejected, fixture-supplied `size` rejected on top of `ConfigDict(extra="forbid")`. |
| RISK-010 | mitigated (with accepted exception) | `alembic/src/config.py:59`; `alembic/src/main.py:310-358`; `alembic/README.md:161-173`; `docs/plans/tasks/WP-001/TASK-004.md` | `ALEMBIC_COMMAND` is still `Literal["upgrade", "downgrade"]` (59), so an invalid value is rejected by type. `main()` still returns exit 1 naming the offending variable through `format_validation_errors`, which renders `loc` and never the input (193-210). The manual substitute checks A-D remain recorded durably. Countermeasure (c) — actually building both images — remains unperformed (no container runtime, user-approved accepted condition) and is recorded in `alembic/README.md:161-173` and `docs/db_schema.md:864-866`. |
| RISK-011 | mitigated | `alembic/Dockerfile:13,23,40`; `scripts/seeding/Dockerfile:12,18,27,56,75` | The security remediation touched both Dockerfiles but did not change the base image: every `FROM` in both files is still `python:3.14-slim` (alembic builder 23, runtime 40; seeding builder 27, tests 56, runtime 75), and no lower version appears. The deferred digest pinning (SEC-004) adds only a comment above the `FROM` lines. `uuid.uuidv7` occurs nowhere in delivered source; fixture IDs remain pre-generated hex literals with no runtime ID generation. |
| RISK-012 | mitigated (with accepted exception) | `scripts/seeding/Dockerfile:12,27,56,75`; `scripts/seeding/tests/conftest.py`; `scripts/seeding/requirements.txt` | No stage of either Dockerfile installs a PostgreSQL server or client package after the security remediation's edits — the only `postgres` mention in the seeding Dockerfile is the comment at line 12 recording that the tests stage runs in an image with no `postgres` executable. `conftest.py` still defines only offline `FakeConnection`/`FakeCursor`/`FakeTransaction` recorders and no test requests a `pytest-postgresql` fixture. The tests stage still gates the runtime stage, but has not actually executed (no container runtime, accepted condition); the substitute host run is recorded in the manifest and `scripts/seeding/README.md`. |
| RISK-013 | mitigated | `0001_initial_base_schema.py` (single match for the grep set: `CREATE TRIGGER` at 194); `docs/db_schema.md:60-64,93` | A search of `alembic/` for `op.create_index`, `index=True` and `CREATE INDEX` returns no match in any delivered file; the only hit for the grep set in the revision is the shared `CREATE TRIGGER` helper. The QC remediation added `COMMENT ON TYPE` statements only. Uniqueness remains expressed exclusively through `sa.UniqueConstraint`, and the object inventory still records "Secondary indexes | 0" (93). |
| RISK-014 | mitigated | `docs/db_schema.md:60-93,103-421,423-500,639-699,725-847,849-877` | Still authored from the revision, and now *more* accurate than at iteration 0: the one statement that did not match the delivered code — the residue claim at §3 — was corrected by the remediation (75-84) and is no longer a documentation defect. Per-table Markdown tables, the 20-entity Mermaid diagram, the `[PG-005]` and `[PG-014]` waivers, every F-9a-f resolution with final widths and the §7.3 record of the nullable `api_key_id` all remain. |
| RISK-015 | mitigated | `alembic/src/config.py:47-59`; `scripts/seeding/src/config.py:62-72`; `alembic/README.md:95-123`; `scripts/seeding/README.md:140-165`; `alembic/src/main.py:130-171,213-271,324-350`; `scripts/seeding/src/main.py:155-196,460-463` | The two `config.py` files have **not** drifted after the QC remediation added logging settings to both. Field-for-field the shared half is identical: `LOG_LEVEL` (`min_length=1`, default `"INFO"`), `LOG_FILE` (`min_length=1`), `POSTGRES_HOST/USER/DB` required `min_length=1`, `POSTGRES_PORT` `int` default `5432` with `gt=0, le=65535`, `POSTGRES_PASSWORD` `SecretStr` in both. The only intentional differences are the per-container `DEFAULT_LOG_FILE` (`/app/logs/migrations.log` vs `/app/logs/seeding.log`) and the migration-only `ALEMBIC_*` fields — both are stated as intentional in both READMEs, whose contract tables are otherwise identical row-for-row (`alembic/README.md:95-101` vs `scripts/seeding/README.md:140-146`) and each of which explicitly says the shared rows "must not diverge". Both new `configure_logging` implementations are structurally identical and neither logs the settings object; the alembic wrapper's `redact()` was strengthened to strip the raw secret *and* both percent-encoded renderings (`quote_plus` and `quote(safe="")`) before any exception text is logged (213-271, applied at 349), and the seeder still reports configuration failures by variable name only (460-463). |
| RISK-016 | mitigated | `0001_initial_base_schema.py` (`base.article.document_id`); `docs/db_schema.md:174,179-189,439-440,645-663` | Unchanged by the remediations: nullable FK with `ondelete="CASCADE"`, no drift to `SET NULL`, and the consequence still documented next to the `[PG-005]` waiver. |
| RISK-017 | mitigated | `0001_initial_base_schema.py:144-199,1151-1172`; `docs/db_schema.md:66-68` | The single shared helpers (`timestamp_columns()`, `create_updated_at_trigger()`) still drive every table through the `TABLES` loop (1171-1172), so the convention cannot vary per table; the forbidden `now() AT TIME ZONE 'utc'` form appears nowhere. **Improved this iteration:** the QC remediation added `COMMENT ON TYPE base.http_method` (1155-1158) and `COMMENT ON TYPE base.document_type` (1161-1164) immediately after each type is created, closing the last gap in the countermeasure's `COMMENT ON` coverage (schema, tables, columns, function, triggers, and now both enum types). `upgrade`'s docstring records that the comment dies with the type, so `downgrade` needs no counterpart. |
| RISK-018 | mitigated | `docs/plans/manifests/WP-001-manifest.md:43-93,99`; `.gitignore:1-9`; repository glob for `docker-compose*` and `.github/**` | The scope boundary held through all three remediations. A repository-wide glob finds no `docker-compose*` file and no `.github/**` path; no PostgreSQL provisioning was added to close the verification gap — it is still only reported. `.pre-commit-config.yaml` and `.flake8` remain absent from the changeset, `docs/specs/**` and `acceptance/**` are untouched, and the root `Makefile` still carries `scan-secrets`/`claude` plus thin delegations. Two files outside the original boundary were added by remediation and are declared in the manifest's Reviewer Attention (99): `.gitignore` (three lines of Python build artefacts, 7-9, preserving the pre-existing entries 1-5) and `.secrets.baseline` (regenerated for the fixture `api_key` hashes). Both are remediation-mandated hygiene files rather than out-of-scope infrastructure, and neither is a user input, so the boundary's intent is intact. |
| RISK-019 | mitigated | `scripts/seeding/README.md` §3; manifest waves 4-11 | Unchanged: the catalogue landed in wave 4 ahead of the model and fixture tasks, covers all six feature files and all three tags, and carries a "Cited by" scenario per row. No remediation altered the catalogue or the fixtures. |
| RISK-020 | mitigated | `docs/plans/tasks/WP-001/TASK-RISK-REMEDIATION.md:51-66`; `alembic/migrations/env.py`; `0001_initial_base_schema.py:11-14,235-250`; `docs/db_schema.md:70-84`; `alembic/README.md:28-33` | The remediation itself is a post-Phase-1 change to the migration component, and it was propagated coherently in one sequence rather than partially: `env.py` (version table location), the revision (unconditional drop, plus the module docstring at 11-14 restating the invariant), `docs/db_schema.md` §3 and `alembic/README.md`. The change is schema-neutral — no width, constraint, enum name or FK action moved — so the seeding models, persistence order and fixture corpus needed no corresponding edit. Both offline renders were re-run and the object counts recorded as unchanged (`TASK-RISK-REMEDIATION.md:61-66`). |
| RISK-021 | mitigated (with accepted exception) | `docs/plans/tasks/WP-001/TASK-RISK-REMEDIATION.md:61-66,73-79`; manifest Task Results 38-41; `docs/db_schema.md:864-866` | Phase-boundary evidence is intact and the review loop itself proved its value: the Phase 1 offline-render checkpoint, re-run after the remediation, is what confirmed `CREATE SCHEMA IF NOT EXISTS base` now precedes any `base` object and that the downgrade ends with `DROP SCHEMA base RESTRICT;`. Red evidence for the pre-fix state is recorded (73-79). The image-build checkpoint still could not be executed (no container runtime, accepted condition) and is recorded as such in both component READMEs. |
| RISK-022 | mitigated | `docs/db_schema.md:849-877`; `alembic/README.md:14,161-199`; `scripts/seeding/README.md:194-233` | The verification gap survived all documentation edits and was **extended** this iteration. `docs/db_schema.md` §10 (849-877) still states AC-1 … AC-10 are implemented but unverified, names both waived mechanisms (no migration CI job at 858-859, no provisioned PostgreSQL instance at 860-862), records the absent container runtime (864-866), states AC-11 as the only closable criterion, and closes with "one of eleven acceptance criteria is verified" plus the required follow-up. Both component READMEs repeat it for their own criteria (`alembic/README.md:161-173` for AC-1 … AC-5, `scripts/seeding/README.md:194-206` for AC-6 … AC-10), each naming the absent container runtime as the reason the image build and `make` targets were not executed. The deferred SEC-004 base-image digest pinning is now recorded durably in both READMEs as its own outstanding section (`alembic/README.md:175-199`, reachable from the table of contents at 14; `scripts/seeding/README.md:209-233`), each stating that it is *not* fixed in this changeset, why (no runtime to pull the image and resolve a digest), and the exact procedure to close it. |

## Remediations Required

None. No risk carries status `deviation-found` in this iteration, so no
`TASK-RISK-REMEDIATION.md` entry is created; the existing task file records that iteration 0's
RISK-001 deviation was resolved by `TASK-CODE-REVIEW-REMEDIATION`.

## Observation (not a deviation)

`alembic/migrations/versions/0001_initial_base_schema.py:220-222` — `create_schema()`'s docstring
still explains the `IF NOT EXISTS` clause by saying alembic's version table "lives in it and may
already have brought the schema into existence". That is stale after the fix and contradicts the
module docstring (11-14), `drop_schema()`'s docstring (238-244) and `env.py:9-15`. The emitted
DDL is correct and unaffected (`CREATE SCHEMA IF NOT EXISTS base` remains harmless and idempotent),
and no risk countermeasure governs source docstrings, so this is recorded as an observation rather
than a mitigation deviation. A one-sentence docstring correction would remove the last trace of
the defect.
